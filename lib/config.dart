import 'package:flutter/material.dart';

class configurations extends StatefulWidget {
  @override
  _configState createState() => _configState();
}

class _configState extends State<configurations> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.blue,
        title:Text('Details',style: TextStyle(fontSize: 20.0, color: Colors.white),),
      ),

      body: Column(

        children: <Widget>[


]



      ),
    );
  }
}
